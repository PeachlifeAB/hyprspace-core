# Summary: Adds new-window-or-open, which opens an app if it is closed and triggers its menu-bar “New Window” action if it is already running.

diff --git a/Sources/AppBundle/command/impl/NewWindowOrOpenCommand.swift b/Sources/AppBundle/command/impl/NewWindowOrOpenCommand.swift
new file mode 100644
index 00000000..44bac8f1
--- /dev/null
+++ b/Sources/AppBundle/command/impl/NewWindowOrOpenCommand.swift
@@ -0,0 +1,45 @@
+import AppKit
+import Common
+import Foundation
+
+struct NewWindowOrOpenCommand: Command {
+    let args: NewWindowOrOpenCmdArgs
+    /*conforms*/ let shouldResetClosedWindowsCache = false
+    /*conforms*/ let shouldRefreshModel = false
+    /*conforms*/ let shouldRunInLightSession = false
+
+    @MainActor
+    func run(_ env: CmdEnv, _ io: CmdIo) async throws -> Bool {
+        guard let app = NSWorkspace.shared.runningApplications.first(where: {
+            $0.localizedName == args.appName.val || $0.bundleIdentifier == args.appName.val
+        }) else {
+            let process = try Process.run(URL(filePath: "/usr/bin/open"), arguments: ["-a", args.appName.val])
+            process.waitUntilExit()
+            return process.terminationReason == .exit && process.terminationStatus == 0
+        }
+        let appName = app.localizedName ?? args.appName.val
+        app.activate(options: .activateIgnoringOtherApps)
+        let script = """
+tell application "System Events"
+    tell process "\(appName)"
+        set frontmost to true
+        delay 0.2
+        set fileItems to name of menu items 1 thru 2 of menu 1 of menu bar item "File" of menu bar 1
+        repeat with itemName in fileItems
+            if itemName contains "New" then
+                click menu item (itemName as text) of menu 1 of menu bar item "File" of menu bar 1
+                return
+            end if
+        end repeat
+        error "no New item found in first two File menu items"
+    end tell
+end tell
+"""
+        let result = try Process.run(URL(filePath: "/usr/bin/osascript"), arguments: ["-e", script])
+        result.waitUntilExit()
+        guard result.terminationReason == .exit && result.terminationStatus == 0 else {
+            return io.err("Could not open new window for '\(args.appName.val)'")
+        }
+        return true
+    }
+}
diff --git a/Sources/AppBundleTests/command/NewWindowOrOpenCommandTest.swift b/Sources/AppBundleTests/command/NewWindowOrOpenCommandTest.swift
new file mode 100644
index 00000000..63c6dcf2
--- /dev/null
+++ b/Sources/AppBundleTests/command/NewWindowOrOpenCommandTest.swift
@@ -0,0 +1,10 @@
+@testable import AppBundle
+import Common
+import XCTest
+
+final class NewWindowOrOpenCommandTest: XCTestCase {
+    func testParse() {
+        testParseCommandSucc("new-window-or-open Ghostty", NewWindowOrOpenCmdArgs(rawArgs: []).copy(\.appName, .initialized("Ghostty")))
+        assertEquals(parseCommand("new-window-or-open").errorOrNil, "ERROR: Argument '<app-name>' is mandatory")
+    }
+}
diff --git a/Sources/AppBundle/command/cmdManifest.swift b/Sources/AppBundle/command/cmdManifest.swift
index af7c3e34..85c955d6 100644
--- a/Sources/AppBundle/command/cmdManifest.swift
+++ b/Sources/AppBundle/command/cmdManifest.swift
@@ -58,6 +58,8 @@ extension CmdArgs {
                 command = MoveNodeToWorkspaceCommand(args: self as! MoveNodeToWorkspaceCmdArgs)
             case .moveWorkspaceToMonitor:
                 command = MoveWorkspaceToMonitorCommand(args: self as! MoveWorkspaceToMonitorCmdArgs)
+            case .newWindowOrOpen:
+                command = NewWindowOrOpenCommand(args: self as! NewWindowOrOpenCmdArgs)
             case .reloadConfig:
                 command = ReloadConfigCommand(args: self as! ReloadConfigCmdArgs)
             case .resize:
diff --git a/Sources/Common/cmdArgs/cmdArgsManifest.swift b/Sources/Common/cmdArgs/cmdArgsManifest.swift
index ddacd836..df193a6d 100644
--- a/Sources/Common/cmdArgs/cmdArgsManifest.swift
+++ b/Sources/Common/cmdArgs/cmdArgsManifest.swift
@@ -29,6 +29,7 @@ public enum CmdKind: String, CaseIterable, Equatable, Sendable {
     case moveNodeToMonitor = "move-node-to-monitor"
     case moveNodeToWorkspace = "move-node-to-workspace"
     case moveWorkspaceToMonitor = "move-workspace-to-monitor"
+    case newWindowOrOpen = "new-window-or-open"
     case reloadConfig = "reload-config"
     case resize
     case split
@@ -104,6 +105,8 @@ func initSubcommands() -> [String: any SubCommandParserProtocol] {
                 result[kind.rawValue] = SubCommandParser(parseWorkspaceToMonitorCmdArgs)
                 // deprecated
                 result["move-workspace-to-display"] = SubCommandParser(MoveWorkspaceToMonitorCmdArgs.init)
+            case .newWindowOrOpen:
+                result[kind.rawValue] = SubCommandParser(NewWindowOrOpenCmdArgs.init)
             case .reloadConfig:
                 result[kind.rawValue] = SubCommandParser(ReloadConfigCmdArgs.init)
             case .resize:
diff --git a/Sources/Common/cmdArgs/impl/NewWindowOrOpenCmdArgs.swift b/Sources/Common/cmdArgs/impl/NewWindowOrOpenCmdArgs.swift
new file mode 100644
index 00000000..33333333
--- /dev/null
+++ b/Sources/Common/cmdArgs/impl/NewWindowOrOpenCmdArgs.swift
@@ -0,0 +1,17 @@
+import Foundation
+
+public struct NewWindowOrOpenCmdArgs: CmdArgs {
+    public let rawArgsForStrRepr: EquatableNoop<StrArrSlice>
+    public init(rawArgs: StrArrSlice) { self.rawArgsForStrRepr = .init(rawArgs) }
+    public static let parser: CmdParser<Self> = cmdParser(
+        kind: .newWindowOrOpen,
+        allowInConfig: false,
+        help: "USAGE: aerospace new-window-or-open <app-name>",
+        flags: [:],
+        posArgs: [newArgParser(\.appName, consumeStrCliArg, mandatoryArgPlaceholder: "<app-name>")]
+    )
+
+    /*conforms*/ public var windowId: UInt32?
+    /*conforms*/ public var workspaceName: WorkspaceName?
+    public var appName: Lateinit<String> = .uninitialized
+}
diff --git a/Sources/Common/cmdHelpGenerated.swift b/Sources/Common/cmdHelpGenerated.swift
index 62012af9..277752bf 100644
--- a/Sources/Common/cmdHelpGenerated.swift
+++ b/Sources/Common/cmdHelpGenerated.swift
@@ -121,5 +121,8 @@ let move_help_generated = """
     USAGE: move [-h|--help] [--window-id <window-id>] [--boundaries <boundary>] [--boundaries-action <boundary-action>] (left|down|up|right)
     """
+let new_window_or_open_help_generated = """
+    USAGE: new-window-or-open <app-name>
+    """
 let reload_config_help_generated = """
     USAGE: reload-config [-h|--help] [--no-gui] [--dry-run]
     """
diff --git a/docs/aerospace-new-window-or-open.adoc b/docs/aerospace-new-window-or-open.adoc
new file mode 100644
index 00000000..306389db
--- /dev/null
+++ b/docs/aerospace-new-window-or-open.adoc
@@ -0,0 +1,14 @@
+= new-window-or-open(1)
+
+== Name
+
+new-window-or-open - trigger a menu item for a running app or launch the app if it is not running
+
+== Synopsis
+
+`aerospace new-window-or-open <app-name>`
+
+== Description
+
+If the target app is already running, open a new window by clicking the first File menu item containing "New".
+If the app is not running, launch it by app name and return.

