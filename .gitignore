/.idea
log/
refcode/
PLAN.md
JOURNAL.md
promises/
AeroSpace/
symlinks/
/.debug
/.release
/.shell-completion
/.site
/.man
/Gemfile.lock
# IDK, AppCode randomly creates this EMPTY file. I have no idea what this is
/default.profraw
.DS_Store
/.xcode-build
/.build
/.swiftpm
/.vscode
/.local-install-test
/Hyprspace/.specify
refcode/

# Local agent memory overrides (per-developer, not committed)
AGENTS.override.md
CLAUDE.local.md
/.gdog
/graft
/.mcp.json

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
